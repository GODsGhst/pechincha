# Expo Go SDK 54

This app runs with Expo SDK 54 and React Native 0.81 through Expo Go.
Do not upgrade to SDK 56/canary unless the installed Expo Go version supports it.

Use the SDK 54 docs when touching Expo APIs:
https://docs.expo.dev/versions/v54.0.0/
